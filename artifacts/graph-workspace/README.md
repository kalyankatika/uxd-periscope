# Graph workspace verification — historical

The separate workspace was subsequently consolidated into the original Work map. These artifacts describe revision `0ec9906`.

September 20, 2026. AI-assisted browser walkthrough using fictional, session-only data. Not a human usability study.

- Head of Design → Elena → Account opening → Maya: focused network and stable breadcrumb verified.
- Person title edited in the example session: inspector updated immediately.
- Capacity opened and returned: selected project and trail retained.
- Related project opened in drawer → View in map: correct new node and trail.
- Fullscreen → Overview → Back to graph: selection retained and body scroll unlocked.
- Mobile 390×844: tools/import entry and return; document width 390px.
- Standard workspace switch: original Work map and all navigation restored.
- Browser errors empty; saved SQLite and sidecar hashes unchanged.
- 64 tests and production build pass.

`browser-actions.jsonl` and `browser-logs/` contain observed actions and results. Desktop/mobile PNGs show the final build. Early logs precede the related-project fix and compact heading refinement. `browser.py` is a local evidence helper and requires the browser CLI path to be adapted on other machines.

Only entry/return of import tools was exercised here; the full import contract remains covered by prior onboarding evidence and automated tests. Navigation is session-only. Whole-workspace tools are not selected-person reports. No enterprise synchronization or autonomous agent execution is claimed.
